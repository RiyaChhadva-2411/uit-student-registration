import React from 'react';
import ReactDOM from 'react-dom';
import './index.css';
import StudentRegistration from "../src/components/StudentReg.js"

ReactDOM.render(
  <React.StrictMode>
    <StudentRegistration />
  </React.StrictMode>,
  document.getElementById('root')
);

